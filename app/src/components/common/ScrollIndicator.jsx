// Scroll progress indicator / navigation dots
export default function ScrollIndicator({ totalSlides = 7, activeSlide = 0 }) {
  return (
    <nav className="scroll-indicator">
      {/* Scroll indicator implementation */}
    </nav>
  )
}
