// Reusable flow chart component for step-by-step visualizations
export default function FlowChart({ steps = [], className = '' }) {
  return (
    <div className={className}>
      {/* Flow chart implementation */}
    </div>
  )
}
